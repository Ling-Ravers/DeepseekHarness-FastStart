# The @deepseek-ai dev-dependency stack ships pre-release packages whose peer
# ranges do not all resolve strictly; the build and tests never combine them at
# runtime, so peer strictness is relaxed for installs.
legacy-peer-deps=true
